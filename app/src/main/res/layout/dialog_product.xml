<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp">

    <EditText
        android:id="@+id/dialogNameInput"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:hint="Product name"
        android:inputType="textCapWords" />

    <EditText
        android:id="@+id/dialogColorInput"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:hint="Color or variant"
        android:inputType="textCapWords" />

    <EditText
        android:id="@+id/dialogStockInput"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:hint="Current stock"
        android:inputType="number" />

    <EditText
        android:id="@+id/dialogThresholdInput"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:hint="Low stock threshold"
        android:inputType="number" />
</LinearLayout>
